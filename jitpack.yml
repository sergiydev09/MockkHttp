install:
  - ./gradlew :gradle-plugin:publishToMavenLocal :android-library:publishToMavenLocal -x test --no-configuration-cache
before_install:
  - sdk install java 17.0.16-tem || true
  - sdk use java 17.0.16-tem
