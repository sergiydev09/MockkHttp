pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "MockkHttp"

include(":gradle-plugin")
include(":android-library")
